# Promptfoo evals (optional)

Minimal regression checks for command output shape. Not blocking daily work.

```bash
cd eval/promptfoo
npx promptfoo eval
```

Cases under `prompts/` target core loop command shapes (`scan`, ship verify verdict).
