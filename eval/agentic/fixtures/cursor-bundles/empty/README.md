# Empty `.cursor` stub (bakeoff SEM arm)

Minimal placeholder so the SEM arm can swap the real project `.cursor` for a bundle with **no** Octo rules/skills.

Do not add `rules/` or `skills/` content here — emptiness is the point.
