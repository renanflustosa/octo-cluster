# BC-01 — Clarify V1 readiness paragraph

## Arm invariant

Use the **same** prompt text on every arm (`nada` / `baseline` / `compress-on` / `octo-full`). One card per chat.

## Prompt (paste into `/scan`)

```text
BC-01: In docs/guides/v1-harness-readiness.md, clarify the "Quick validate" section with one short sentence stating that WARN layers are manual checks and do not fail productivity-audit. Do not change other sections. Keep English. Scope: that file only.
```

## Target

- File: `docs/guides/v1-harness-readiness.md`
- Type: docs (tiny)

## Acceptance

- [ ] One clarifying sentence added near Quick validate
- [ ] No unrelated edits
- [ ] `/close` records `combination_id` for the active arm
