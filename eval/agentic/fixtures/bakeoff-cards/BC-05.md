# BC-05 — Metrics README combination clarity

## Arm invariant

Same prompt on every arm. One card per chat.

## Prompt (paste into `/scan`)

```text
BC-05: In eval/metrics/README.md, under the Combination bakeoff section, add one sentence that when usage_source is skipped, ranking still uses harness_score and LOC proxies. English only. Scope: that file only.
```

## Target

- File: `eval/metrics/README.md`
- Type: docs (tiny)

## Acceptance

- [ ] One sentence on skipped usage + LOC/score proxies
- [ ] No unrelated edits
- [ ] `/close` records `combination_id`
