# BC-03 — Catalog comment clarity

## Arm invariant

Same prompt on every arm. One card per chat.

## Prompt (paste into `/scan`)

```text
BC-03: In docs/architecture/harness-catalog.yaml, add or clarify a single YAML comment near the okf-offline-index entry stating sessionStart injection stays deferred (ADR-005). Do not change tool ids or toggles. Scope: that file only.
```

## Target

- File: `docs/architecture/harness-catalog.yaml`
- Type: docs (tiny)

## Acceptance

- [ ] One comment clarifies ADR-005 deferral
- [ ] No toggle/id renames
- [ ] `/close` records `combination_id`
