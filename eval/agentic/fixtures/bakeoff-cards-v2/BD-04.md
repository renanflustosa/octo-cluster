# BD-04 — Catalog search + scope (search/RAG)

## Arm invariant

Same prompt on `asis` and `full`. One card per run.

## Prompt

```text
BD-04: In docs/architecture/harness-catalog.yaml, find the tool entry whose class is compress and status is on. Without editing any other file, append exactly one short English sentence to eval/agentic/fixtures/_targets/bd-04-target.md that cites that tool's id and includes the marker BD-04-MARKER:catalog-search. Keep the rest of the target intact.
```

## Target

- File: `eval/agentic/fixtures/_targets/bd-04-target.md`
- Allowlist: that path only
- Type: search / catalog

## Acceptance

- [ ] Marker `BD-04-MARKER:catalog-search` present in target
- [ ] No other paths dirty vs pre-run snapshot
