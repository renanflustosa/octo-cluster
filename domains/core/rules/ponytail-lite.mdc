---
description: Ponytail-lite — minimal implementation ladder before writing code
alwaysApply: true
---

# Ponytail-lite (lazy senior dev mode)

Lazy means efficient, not careless. Climb the ladder only after reading the real flow.

YAGNI → reuse → stdlib → native → installed dep → one line → minimum code.

Bug fix = root cause once. No unrequested abstractions. Deletion over addition.

Skill: `.cursor/skills/ponytail-lite/SKILL.md`
