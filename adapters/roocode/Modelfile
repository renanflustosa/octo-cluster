# Use the lightweight version that fits in your 2GB MX550 GPU
FROM qwen2.5-coder:1.5b

# Sets a strict 4k context window so it never spills over into slow system RAM
PARAMETER num_ctx 4096

# Slightly lower temperature keeps coding logical and prevents hallucinations
PARAMETER temperature 0.1

# FORCES Ollama to load all layers onto your Nvidia MX550
PARAMETER num_gpu 99