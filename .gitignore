.DS_Store
Thumbs.db
*.tmp
.env
.env.*

# Machine-local state (memory indexes, backups, metrics)
state/memory/
state/backups/
state/logs/
state/metrics/
state/*.md

# Context engine dependencies and caches
engine/context-engine/node_modules/
engine/context-engine/.cache/

# Transformers model cache (large)
**/.cache/

generated/

# OS / editor
*.log

# Local overlays (generic — no consumer identifiers)
contexts/runtime/*.local.json
contexts/runtime/*.private.json
capabilities/registry.local.yaml
repo-policies/*.private.yaml
repo-policies/*.local.yaml

# Private pack roots (local fork or machine-only)
domains/_private/
capabilities/_private/
contexts/_private/
docs/_private/

# Bakeoff / SDK local artifacts
eval/agentic/results/
eval/agentic/fixtures/_sdk-runner/

__pycache__/
*.py[cod]
